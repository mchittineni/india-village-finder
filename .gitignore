# Python tooling
scraper/.venv/
**/__pycache__/
**/.pytest_cache/
*.pyc

# Large, re-downloadable source dumps (regenerated by the pipeline)
scraper/.cache/

# Node (mapshaper boundary simplification + JSDoc/Prettier tooling)
node_modules/
package-lock.json

# Generated API reference (built on demand / in CI by docs.yml)
docs/api/

# OS
.DS_Store
**/.DS_Store

# regenerable artifacts published to data/* branches by CI (overlaid into the
# site/releases at deploy time) — see the README's review-flow section
tiles/boundaries.pmtiles
*/web/data/boundary_bounds.json
*/web/data/parcels_index.json
*/web/data/village_points.json
scraper/osm_names.json
